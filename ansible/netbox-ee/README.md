`ansible-builder build --container-runtime docker`

Default name : ansible-execution-env unless you use `-t my_ee_name`

The aim of this execution environment is to successfully run nb_lookup in playbooks on awx as well as plugin modules to update my netbox. 


Use `ansible-navigator` to inspect the details of images
